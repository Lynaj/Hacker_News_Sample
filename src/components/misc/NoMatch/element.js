import React, { Component, Fragment } from 'react';

export default class NoMatch extends React.Component {
    render() {
      return (
            <Fragment>
                NoMatch
            </Fragment>
        )
    };
}